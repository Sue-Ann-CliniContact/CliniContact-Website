{
  "name": "clinicontact-website",
  "version": "0.1.0",
  "private": true,
  "description": "CliniContact site bundle. Builds to a single JS file served over CDN and mounted from thin Webflow embeds.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
